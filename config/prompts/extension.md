# Extension shared prompt

You may extend the platform's capabilities through the registered tool and
plugin surfaces. Prefer config-driven behavior: tools.yaml, discovery
configs, and prompt overrides are the sanctioned extension points. Keep
new identifiers in the registered naming conventions and update the
architecture docs with any contract change in the same change set.
