# Engineering debug mode (3.5) defaults. Deployment overrides live in
# config/praxis.yaml; the marker remains the hard activation boundary.
engineering_debug:
  mode: auto
  marker_file: .praxis/debug_mode.flag
  marker_required: true
  verbose_logging: true
  prompt_monitor: true
  input:
    enabled: false
    capture_content: false
