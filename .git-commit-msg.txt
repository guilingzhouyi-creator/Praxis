refactor(l2): single help source, bridge-only reach-ins, honest modes

the duplicate /help body collapses to a forward to connect.py (its
pipeline tip now describes the real behavior); cell agent resolution
and provider health cross through l2.bridge dict functions instead of
touching _agents/_provider internals; card/session display caps move
into params; /kill reports unknown agents instead of silent success;
/connect routes bare ids across cells via the selector; /mode tool
persists read/write on the session instead of discarding it.

Co-Authored-By: OpenCode (x-preview-f-free) <noreply@opencode.ai>
