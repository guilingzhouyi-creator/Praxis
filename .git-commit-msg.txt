test(l2): lift coverage to 75 percent and clear protocol type errors

four handler-contract batches cover the session/model/skills/memory
dispatch tables, selector routing gates, terminal dialect helpers, and
concurrent-host behavior over mocked L1/L3 boundaries; records and
projection typing now satisfies mypy; the acb bridge import joins the
layer-import allowlist; doc stats snapshots regenerate in sync.

Co-Authored-By: OpenCode (x-preview-f-free) <noreply@opencode.ai>
