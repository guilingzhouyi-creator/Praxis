perf(l2): unblock concurrent dispatch and single-scan preselect

protocol host captures legacy prints through a thread-local stdout
proxy instead of a process-global redirect lock, so sessions dispatch
in parallel; host indexes gained an RLock for threaded web mode; the
role-index builder reuses the liveness snapshots preselect already
fetched (one scan per cell, not two); status/vfs/clear handlers return
data instead of writing stdout, keeping the dict contract the TS
frontend renders from.

Co-Authored-By: OpenCode (x-preview-f-free) <noreply@opencode.ai>
